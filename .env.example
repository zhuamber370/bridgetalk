# ─── Adapter 选择优先级 ───
# 1. OPENCLAW_GATEWAY_URL 存在 → OpenClawAdapter（核心）
# 2. KIMI_API_KEY 存在 → KimiAdapter（调试用）
# 3. 都没有 → MockAdapter（开发用）

# ─── OpenClaw Gateway 配置（核心 Adapter）───
# WebSocket 原生协议（推荐）
OPENCLAW_GATEWAY_URL=ws://127.0.0.1:18789
OPENCLAW_GATEWAY_TOKEN=your-token-here
# OPENCLAW_GATEWAY_TIMEOUT=300000  # 超时毫秒数，默认 300000 (5分钟)
# 注意：如果填写 http:// 或 https:// 会自动转换为 ws:// 或 wss://

# ─── Kimi K2.5 配置（调试用，月之暗面 Moonshot AI）───
# 获取 API Key: https://platform.moonshot.ai/console/api-keys
# KIMI_API_KEY=sk-your-api-key-here
# KIMI_BASE_URL=https://api.moonshot.ai/v1
# KIMI_MODEL=kimi-k2.5
